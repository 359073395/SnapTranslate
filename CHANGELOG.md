# Changelog

## 0.3.0

- 软件品牌升级为“灵犀截图 / Lingual Insight”，统一窗口标题、应用图标、托盘图标、EXE 元数据和导出文件名。
- 使用用户提供的 LI 蓝色图标，并将主界面强调色切换为品牌钴蓝 `#2A5CFF`。
- 点击关闭按钮后不再保留任务栏窗口，而是隐藏到系统托盘；托盘菜单支持显示主界面、开始截图和退出程序。
- 新增单实例唤醒：重复启动时会显示已经在托盘运行的主界面。
- 新增“开机自动启动”设置，默认开启，并以后台托盘模式启动。
- 设置目录迁移到 `%APPDATA%\LingxiCapture`，首次启动会自动继承原 `%APPDATA%\SnapTranslate` 配置和加密 API Key。
- 新增 Windows 安装向导，可自行选择安装路径，并创建开始菜单/可选桌面快捷方式。

## 0.2.5

- 微信式浮动工具栏新增圆形、箭头和马赛克按钮，顺序保持为矩形 → 圆形 → 箭头 → 画笔 → 马赛克 → 文字。
- 圆形支持自由拖拽绘制椭圆，箭头根据起点和终点自动调整方向与箭头大小。
- 马赛克使用原图采样的像素块连续涂抹，可通过粗细选项调整笔刷范围。
- 三种新标注都支持撤销、保存 PNG、完成复制和带入高级编辑。
- 修复工具按钮获得焦点时，按回车可能重复触发当前工具而不是完成复制的问题。

## 0.2.4

- 参考微信截图重排日常工具栏：标注工具在左，OCR/翻译居中，撤销/保存/高级编辑在后，取消和完成固定在最右。
- 移除多余的“复制图片”按钮；点击绿色完成、按回车或双击选区会自动复制最终图片并结束截图。
- `Esc` 明确用于取消截图，取消和完成分别使用红色叉号与绿色勾号。
- 剪贴板写入增加重试和刷新，避免截图完成后图片没有留在剪贴板。
- 工具栏改为更接近微信的紧凑纯图标样式，并保留悬停提示和辅助功能名称。

## 0.2.3

- 新增贴近选区的微信式日常截图工具栏，标注、OCR、翻译、复制、保存和完成无需进入独立编辑器。
- “复制图片”与“保存 PNG”并列；“复制原文”和“复制译文”分别放在对应结果区域。
- 支持在选区内按回车或双击，复制带标注及译文覆盖层的最终画面并结束截图。
- 新增“高级编辑”入口，把当前最终画面带入原来的完整编辑器继续处理。
- 点击主窗口右上角关闭按钮时改为最小化，截图快捷键继续在后台可用。
- 设置页新增“退出程序”按钮，用于明确结束 SnapTranslate。
- 修复自动 OCR 已保存但重新启动后界面可能回退到中文的问题。
- 调整设置页底部按钮与状态布局，避免状态文字和退出操作互相挤压。

## 0.2.2

- OCR 语言新增“自动识别（推荐）”，并作为新安装的默认选项。
- 自动模式会依次调用本机已安装的候选 OCR 引擎，根据有效文字、覆盖范围和文字体系选择更合适的结果。
- 支持中文、英文/印尼语和中英混排截图自动切换，无需每次手动选择 OCR 语言。
- 编辑器完成识别后会显示自动选中的文字体系，仍可在设置中固定使用某个 OCR 语言。

## 0.2.1

- OpenAI 兼容接口新增遮罩 API Key 输入框，并使用 Windows 当前用户的数据保护机制加密保存。
- 接口地址支持直接填写 `/v1` 基础 URL，程序会自动补全 `/chat/completions`。
- 新增“测试连接”按钮，可在截图前验证 URL、Key 和模型配置。
- HTTP 请求失败时显示中转站返回的状态码和错误信息，便于排查。
- GPT-5 系列翻译请求使用低延迟的无推理模式，不再发送旧模型的 `temperature` 参数。
- 保留 `SNAPTRANSLATE_API_KEY` 环境变量作为兼容回退。

## 0.2.0

- Windows OCR 结果现在保留每行文字在截图中的坐标。
- “翻译到图片”会逐行翻译，并把译文覆盖到原文字块位置。
- 新增独立译文图层，可随图片复制或保存，也可一键清除。
- 目标语言明确显示“印尼语 / Bahasa Indonesia”。
- OpenAI 兼容接口针对印尼 TikTok 文案采用更自然、简洁的翻译提示。
- 多行翻译优先批量请求，行数不一致时自动并发回退，保持译文与位置对应。

## 0.1.1

- 重做深色 ComboBox 模板，修复设置文字对比度不足。
- 支持点击输入框录入自定义全局截图快捷键。
- 快捷键录入后立即注册，保存后可跨启动保留。
- 新快捷键注册失败时保留原快捷键，并显示占用提示。
- 增加无重复触发标志，避免按住快捷键时连续截图。

## 0.1.0

- 首个公开预览版。
- 支持区域截图、矩形/画笔/文字标注、Windows OCR、文字复制、翻译和 PNG 保存。
